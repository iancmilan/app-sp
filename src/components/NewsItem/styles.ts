import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    newsWrapper: {
        padding: 5,
        borderColor: 'grey',
        borderWidth: 1,
        borderRadius: 5,
        marginTop: 15,
    },
    newsTitle: {
        fontSize: 18,
    },
    newsSummary: {

    }
});

export default styles;